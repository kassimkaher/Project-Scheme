# Project lifecycle

Bootstrap from the questionnaire, validate the generated project profile, and confirm selected skills. During delivery, an implementation session reads local state and relevant capabilities, changes the product, runs focused tests, and requests independent QA where configured. Update state only after accepted work. At release, apply the profile-appropriate release gate and preserve QA evidence.
