# Capability completeness

Evaluate a capability end-to-end: domain data, writes, reads, identifiers, lifecycle transitions, authorization, client contract, media, side effects, and QA proof. Define the real actor journey before implementation. A feature is incomplete when a necessary stage has no usable, authorized path or no independent proof.
