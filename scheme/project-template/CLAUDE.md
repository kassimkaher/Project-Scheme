# {{PROJECT_NAME}} session contract

Before implementation, read `.ai/project.yaml` and `.ai/state.md`. Read only the capability relevant to the request, then consult architecture and decisions when needed. Load only these selected Project Scheme skills:

{{SKILLS}}

Confirm the current contract or live interface where relevant, implement the requested work, run focused tests and independent QA when configured, then update `.ai/state.md` after accepted completion.
