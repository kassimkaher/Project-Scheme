# Architecture

Project profile: `{{PROFILE}}`.

Use `{{ARCHITECTURE}}` organization. Record significant boundaries and decisions in `decisions.md`; do not add layers unless the product profile needs them.
