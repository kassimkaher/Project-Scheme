# Project lifecycle

Bootstrap an empty or existing project, validate the smallest sufficient profile, and confirm selected Skills. A feature session reads state and the relevant capability, implements, tests, requests independent focused QA, resolves defects, and updates state only after acceptance.
